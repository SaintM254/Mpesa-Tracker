{
  "name": "mpesa-tracker",
  "private": true,
  "version": "3.1.0",
  "description": "Android app that tracks M-PESA transactions from SMS",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  }
}
